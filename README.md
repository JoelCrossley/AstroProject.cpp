Save the AstroObjects.txt file to the cmake-build-debug folder. Run using the following line: 

g++ -Wall -g Cpp.cpp -o Cpp.exe -std=gnu++17

If adding new data via the terminal, it must be in the same format as the AstroObjects.txt data file
